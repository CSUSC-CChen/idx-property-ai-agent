{
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "mysql2": "^3.22.5",
    "openai": "^7.1.0"
  },
  "scripts": {
    "test": "for f in tests/*.test.ts; do echo \"\\n=== $f ===\"; ./node_modules/.bin/tsx \"$f\" || exit 1; done",
    "typecheck": "tsc --noEmit"
  }
}
